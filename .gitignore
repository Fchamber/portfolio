# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist-ssr
*.local

# NOTE: dist/ is intentionally committed so Cloudflare Pages deploys it
# directly (no build step). Rebuild + commit dist/ after source changes.
# Long-term fix: set a Pages build command (npm run build) and re-ignore dist/.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env (keep .env.example)
.env
.env.*.local

# Local tooling / state
.wrangler/
.superpowers/
.claude/settings.local.json

# Windows download artifacts
*:Zone.Identifier

# Stray CV source (served version lives in public/assets/cv.pdf)
/Flynn-Chambers-CV.docx
